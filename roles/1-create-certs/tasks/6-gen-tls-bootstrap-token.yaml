---

# Create the TLS Bootstrap Token
# This section will walk you through the creation of a TLS bootstrap token that will be used \
#  to bootstrap TLS client certificates for kubelets.

# https://kubernetes.io/docs/admin/kubelet-tls-bootstrapping/

- name: Create TLS bootstrap Token
  script: gen-tls-bootstraptoken.sh
  ignore_errors: false
