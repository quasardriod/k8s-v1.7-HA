---
- name: Copy bootstrap and kube-proxy kubeconfig files to workers
  copy:
    src: /var/tmp/k8s-v1.7-certs/{{item}}
    dest: /home/{{ansible_ssh_user}}/
  with_items:
    - bootstrap.kubeconfig
    - kube-proxy.kubeconfig
  ignore_errors: flase
